package question4;

public class Main {

	public static void main(String[] args) {
		Enemy enemy = new Enemy("정찰병");	// 부모 클래스 객체
		Orc orc = new Orc("그롬"); 		// 자식 클래스 객체
		
		enemy.info();	// 부모 메서드 호출
		orc.info();		// 자식이 재정의한 메서드 호출
	}

}
