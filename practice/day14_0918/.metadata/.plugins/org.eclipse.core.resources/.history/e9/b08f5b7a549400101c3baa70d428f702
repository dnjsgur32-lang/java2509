package com.ljh.trycatch;

public class TryMain {
	/*
	 * public TryMain() {
	 * 
	 * // ArrrayIndexOutBoundsException try { int[] myNums = { 1, 2, 3 };
	 * System.out.println(myNums[10]); } catch (Exception e) { // 자바가 던져주는 오류 메세지
	 * e.printStackTrace(); System.out.println("너의 인덱스는 틀렸어. 너는 배열이 3개 뿐이야."); }
	 * finally { System.out.println("try~catch 구문을 나가기전에 " +
	 * "해야 할 일을 여기에서 작성한다. 끝~");
	 * System.out.println("==========================================="); }
	 */
		
	/*
	 * // throw 키워드 : Exception 개발자 정의 오류. checkAge(15); }
	 */
	
	//	다중 try~catch 구문
	try {
		int[] nums = {1, 2, 3};
	}catch (ArithmeticException e) {
		System.out.println("ArithmeticException");
	}catch() {
		
	}
	
	
	
	private void checkAge(int age) {
		
		if(age < 18) {
			throw new ArithmeticException("18세 미만은 입장이 제한됩니다.");
		}else {
			System.out.println("18세 이상이군요. 입장을 허용합니다.");
		}
	}
	
	
}
